# database lives outside git — back it up via /backup or by copying data/app.db
data/app.db
data/app.db-shm
data/app.db-wal
node_modules/
.claude/
# temp exports from the old Google Sheet (huge)
_*.csv
